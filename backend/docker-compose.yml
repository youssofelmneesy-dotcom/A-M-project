services:
  mongo:
    image: mongo:7
    container_name: am-barbershop-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: am_barbershop
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
